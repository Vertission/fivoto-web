import { AD, CATEGORY, SEARCH, LOCATION, FIELD, QUERY_ME } from './query';
import { CREATE_AD, UPDATE_AD } from './mutation';

export default {
  query: {
    AD,
    CATEGORY,
    SEARCH,
    LOCATION,
    FIELD,
    QUERY_ME,
  },
  mutation: { CREATE_AD, UPDATE_AD },
};
